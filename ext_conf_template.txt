	# cat=bootstrap/enable/10; type=boolean; label=Container: Shows a select-field for a .container or .container-fluid class in any content element if enabled (clear cache).
container = 1

	# cat=bootstrap/enable/20; type=boolean; label=Spacing: Shows select-fields for padding and margin in any content element if enabled. You can set those classes also in the input-field "Extra Class" (clear cache).
spacing = 1

	# cat=bootstrap/enable/30; type=boolean; label=Color: Shows palette "Bootstrap Color" in almost any content element if enabled. You can set those classes also in the input-field "Extra Class" (clear cache).
color = 1

	# cat=bootstrap/enable/40; type=boolean; label=Custom scss: Override default Bootstrap values in "uploads/tx_t3sbootstrap/custom.scss" (clear cache & open BE Modul "T3SB Config").
customScss = 0

	# cat=bootstrap/enable/50; type=string; label=Path to your custom.scss file: (clear cache)
customScssPath = uploads/tx_t3sbootstrap/

	# cat=bootstrap/enable/60; type=boolean; label=Config Override: you can override the "T3SB Config" by TypoScript Constants (clear cache).
configOverride = 0



	# cat=other/enable/10; type=boolean; label=Codesnippet: Enable CKEditor plugin "Code Snippet" (clear cache).
codesnippet = 0

	# cat=other/enable/20; type=boolean; label=CType in class: Shows the CType as ".class" for each content element in the sourcecode if enabled (clear cache).
cTypeClass = 0

	# cat=other/enable/30; type=boolean; label=Expanded Content: you'll have two more (on top and bottom) backend-layout fields (colPos 20 and 21) if enabled (clear cache).
expandedContent = 0

	# cat=other/enable/40; type=boolean; label=Metatags & Browsertitle: disable if using an SEO extension like metaseo or cs_seo (clear cache).
metatags = 1
